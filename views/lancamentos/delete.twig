{% extends "layouts/site.twig" %}
{% block content %}
  <section class="home-section">
        <div class="container pt-5">
            <h2>Lançamentos</h2>
            <form method="post" >
                <div class="row">
					{% if lancamento is defined %}
                    	<div class="mb-3 card-delete">
							<h3>Deletar</h3>
    						<p>Tem certeza que deseja deletar a lançamento <b>{{lancamento.nome}}</b> #{{lancamento.id}}</p>
                			<button type="submit" class="btn btn-primary">Deletar</button>

                		</div>
					{% endif %}
                </div>


				{% if success ==  true %}
					<div class='alert alert-success' role='alert'>
						Lançamento deletado com sucesso!
					</div>
				{% endif %}
                {% if success == false and message is defined %}

					<div class="alert alert-danger">
						{{ message }}
					</div>
                {% endif %}
                <a href="/lancamentos" class="btn btn-primary">Voltar</a>
            </form>
        </div>

    </section>

{% endblock %}
